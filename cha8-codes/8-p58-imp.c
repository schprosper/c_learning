#include <stdio.h>
int main( )
{
	int c[2][4]={{1,2,3,4},{11,12,13,14}},i,*p;
	int (*b)[4]; //指向数组的指针
	
    for (b = c; b < &c[2]; b++)	{
		printf("(*b)[1]=%d\n",(*b)[1]);	
	}
	
    for (i = 0; i < 2; i++)	{
		printf("c[%d][1]=%d\n", i, c[i][1]);	}
	
    printf("%d\n",(*c)[1]); ; //c为指向数组的指针(第一行)c[0]，(*c)[1]为数组c[0]的第2个元素值
	
    printf("%d\n",(*(c+1))[1]); // c+1为指向数组的指针(第二行)c[1]，(*(c+1))[1])为数组c[1]的第2个元素值
    
    return 0;
}
